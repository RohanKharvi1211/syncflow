-- Rollback: Convert companies table back from UUID to INTEGER
-- Note: This is a destructive operation and may lose data if UUIDs don't map back

-- This rollback is complex and may not be fully reversible
-- In practice, you may want to restore from backup instead

-- For now, we'll just note that rollback is not recommended
-- If rollback is needed, restore from a backup taken before this migration


